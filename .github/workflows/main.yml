on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    name: run test
    steps:
    - name: Hello world action step
      id: hello
      uses: onihusube/harmony@v1
      with:
        who-to-greet: 'Mona the Octocat'
